Create one 2x2 sheet of FOUR distinct directional slope terrain sprites by painting INSIDE the four supplied polygons in image 1. Image 1 is an EXACT geometry guide, keep its canvas 1536x1024, every polygon vertex, outline, scale, skew and position pixel aligned. Image 2 is the grass MATERIAL AND STYLE reference only. Do NOT copy image 2's flat diamond geometry. Each polygon in image 1 is the top surface of a different slope orientation; the unusual parallelogram shapes are intentional and MUST NOT be regularized into diamonds. Paint the same hand-painted semi-realistic Han China muted moss grass and fine dry-earth microtexture inside each polygon. No pixel art. Near uniform soft ambient lighting, faint slope lighting allowed only if it stays compatible at adjoining edges. No borders, sides, thickness, cliffs, dark perimeter, drop shadows, grids, labels, text, raised grass clumps or objects. Background and all gutters remain flat pure magenta #FF00FF. Grass textures must reach edges continuously with no rim. Do not move, resize or add any polygon. EXACTLY four polygonal top surfaces as guide.
